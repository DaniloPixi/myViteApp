rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /plans/{planId} {
      allow read, write: if request.auth != null;
    }
    match /memos/{memoId} {
      allow read, write: if request.auth != null;
    }
  }
}
